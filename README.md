# IDEA-JAVA

IntelliJ IDEA Java 项目占位仓库。

## 说明

该仓库用于存放和同步 IntelliJ IDEA 中的 Java 项目。目前已备好 `.gitignore`，可直接推送源码。

## 添加内容

```bash
# 将你的 Java 项目文件放入本目录后
git add .
git commit -m "add Java project source"
git push
```
